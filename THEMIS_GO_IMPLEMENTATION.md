# Themis (Go 版本) 实现与集成文档

本文档详细说明了 Themis 公平交易排序算法在 Bamboo/Phalanx 共识引擎中的 Go 语言实现细节及集成方案。

## 1. 核心架构

Themis 的集成遵循模块化设计，主要分为三层：
- **算法层 (`themis/go_src`)**：纯 Go 实现的图论与公平排序算法。
- **协议层 (`message/message.go`)**：定义副本间交换排序提议的消息结构。
- **集成层 (`replica/replica.go`)**: 在 HotStuff/Phalanx 共识流程中插入排序逻辑。

---

## 2. 算法层实现 (`x f/go_src/`)

### 2.1 数据结构 (`types.go`)
- **`OrderedList`**: 存储交易标识符 (`crypto.Identifier`) 及其本地到达时间戳 (`int64`, 纳秒)。
- **`Sort()`**: 提供确定性的排序方法，确保在计算前所有本地提议均按时间戳线性排序。

### 2.2 公平排序逻辑 (`orderer.go`)
- **`FairOrderer`**:
    1. **交易过滤**：仅处理在至少 $f+1$ 个副本中出现的 "Solid" 交易，确保活性。
    2. **约束构建**：统计所有副本对。如果至少 $f+1$ 个副本认为交易 A 在 B 之前，则在图中添加 A → B 的有向边。
    3. **冲突解决**：将构建好的约束图交给 `TopologyGraph` 进行全局排序。

### 2.3 图算法 (`graph.go`)
- **Tarjan SCC**: 识别交易间的循环依赖（强连通分量）。在公平排序中，不同副本的观测差异可能导致 $A \to B \to C \to A$ 的循环。
- **Hamilton Path**: 在 SCC 内部，使用回溯法寻找哈密尔顿路径。这确保了即使在循环依赖中，系统也能得出一个一致且确定性的顺序。
- **拓扑排序**: 在 SCC 之间进行标准的拓扑排序，形成最终的全局交易序列。

---

## 3. 共识流程集成 (`replica/`)

### 3.1 副本提议阶段
在进入新视图 (`processNewView`) 时：
1. 每个副本根据本地接收交易的时间戳生成 `ThemisProposal`。
2. 副本将该提议发送给当前视图的主节点 (Leader)。

### 3.2 主节点汇总与计算
主节点在 `proposeBlock` 函数中：
1. **等待阶段**：根据配置的 `proposal_wait` 时间，等待其他副本的 `ThemisProposal`。
2. **汇总阶段**：检查是否收集到了至少 $n-f$ (即 $2f+1$) 个副本的提议。
3. **计算阶段**：调用 `FairOrderer.ComputeFairOrder` 计算全局公平顺序。
4. **重排阶段**：根据计算出的顺序，对从内存池抽取的交易进行重新排序，然后打包进区块。

---

## 4. 配置与启用

### 4.1 配置文件 (`bin/config.json`)
通过 `themis` 模块进行参数控制：
```json
"themis": {
  "enabled": true,
  "proposal_wait": 100
}
```
- **`enabled`**: 布尔值，控制是否开启公平排序。
- **`proposal_wait`**: 毫秒数，主节点在计算排序前等待副本提议的超时时间。

---

## 5. 验证与测试

### 5.1 单元测试
- **`graph_test.go`**: 验证了简单线性依赖和复杂 SCC 循环依赖下的排序准确性。
- **`orderer_test.go`**: 模拟多副本环境，验证 $f+1$ 投票逻辑的正确性。

### 5.2 编译验证
通过 `go build ./...` 确保所有模块类型安全且接口适配正确。

---

## 6. 结论
目前的 Go 实现完整复现了 Themis 论文中的公平性语义，并通过模块化插入的方式，在不破坏 Bamboo 原有 HotStuff/Phalanx 逻辑的前提下，提供了抗主节点操纵交易顺序的能力。
